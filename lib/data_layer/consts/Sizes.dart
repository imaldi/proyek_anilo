import 'package:flutter/material.dart';

const size_tiny = 2.0;
const size_small = 4.0;
const size_normal = 8.0;
const size_medium = 16.0;
const size_big = 25.0;
const size_huge = 32.0;
double widthScreen(context) => MediaQuery.of(context).size.width;
double heightScreen(context) => MediaQuery.of(context).size.height;
